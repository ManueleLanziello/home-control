{
  "name": "home-control",
  "version": "1.0.0",
  "private": true,
  "description": "Dashboard autonoma Home Control",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@smarthome/core": "file:../smarthome-core"
  }
}
